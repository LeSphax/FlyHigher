using UnityEngine;
using System.Collections;

public class CameraScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//Screen.SetResolution (400, 600, true);
		//Screen.SetResolution (360, 640, false);

	}
	
	// Update is called once per frame
	void Update () {
	}
}
