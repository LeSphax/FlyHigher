using UnityEngine;
using System.Collections;

public class MoveAvionCombat : MonoBehaviour {
	/*
	public float movementSpeed;
	
	void Update()
	{
		//Vector3 translation = Quaternion.Euler(transform.localEulerAngles) * Vector3.right * 1f;
		Vector3 translation=Quaternion.identity* Vector3.right* 1f;
		
		transform.position += Time.deltaTime * movementSpeed * translation;
	}

*/
}
